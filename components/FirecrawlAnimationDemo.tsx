import React from 'react';

const FirecrawlAnimationDemo: React.FC = () => {
  return (
    <main className="min-h-screen bg-[#05070d] text-neutral-100 flex items-center justify-center px-6">
      <section className="w-full max-w-3xl rounded-xl border border-neutral-800 bg-[#0f1118] p-8 text-center space-y-4">
        <p className="mono text-[10px] uppercase tracking-[0.24em] text-blue-400">// FIRECRAWL_ANIMATION_DEMO</p>
        <h1 className="text-3xl font-bold uppercase tracking-[0.04em]">Demo route placeholder</h1>
        <p className="text-sm leading-relaxed text-neutral-300">
          This route is defined so the application stays type-safe and buildable. It intentionally remains minimal.
        </p>
      </section>
    </main>
  );
};

export default FirecrawlAnimationDemo;
